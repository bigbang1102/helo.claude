---
description: v26.5.16 G-CMD | "Guided Oracle birth and awakening ritual. Default is Soul Sync (~20min), or --fast (~5min). Use when creating a new Oracle in a fresh repo, when user says 'awaken', 'birth oracle', 'create oracle', 'new oracle', or wants to set up Oracle identity in an empty repository. Do NOT trigger for general repo setup, git init, or project scaffolding without Oracle context."
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /awaken

Execute the `awaken` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "awaken"` instead of reading the file manually.

**Otherwise**: Read the skill file at `C:\Users\Dev\.claude\skills/awaken/SKILL.md` and follow ALL instructions in it.

---
*arra-oracle-skills-cli v26.5.16*
