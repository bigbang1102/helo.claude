---
description: v26.5.16 G-CMD | 'Create a new oracle via maw bud — yeast-colony reproduction. Use when user says "bud", "new oracle", "create oracle", "spawn oracle", or wants to create a new permanent oracle from the current one.'
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /bud

Execute the `bud` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "bud"` instead of reading the file manually.

**Otherwise**: Read the skill file at `C:\Users\Dev\.claude\skills/bud/SKILL.md` and follow ALL instructions in it.

---
*arra-oracle-skills-cli v26.5.16*
