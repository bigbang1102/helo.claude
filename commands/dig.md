---
description: v26.5.16 G-CMD | Mine Claude Code sessions — timeline, gaps, repo attribution, session history. Use when user says "dig", "sessions", "past sessions", "timeline", "what did I work on", or wants to see session history. Do NOT trigger for finding code/projects (use /trace), exploring repos (use /learn), or current session status (use /recap).
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /dig

Execute the `dig` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "dig"` instead of reading the file manually.

**Otherwise**: Read the skill file at `C:\Users\Dev\.claude\skills/dig/SKILL.md` and follow ALL instructions in it.

---
*arra-oracle-skills-cli v26.5.16*
