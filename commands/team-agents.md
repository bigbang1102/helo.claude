---
description: v26.5.16 G-CMD | Spin up coordinated agent teams for any task. Reusable framework for TeamCreate/SendMessage/TaskList patterns. Use when user says "team-agents", "spin up a team", "use teammates", "parallel agents", "coordinate agents", "fan out", or wants multiple agents working together with coordination. Do NOT trigger for simple subagent work (use Agent tool directly) or inter-Oracle messaging (use /talk-to).
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /team-agents

Execute the `team-agents` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "team-agents"` instead of reading the file manually.

**Otherwise**: Read the skill file at `C:\Users\Dev\.claude\skills/team-agents/SKILL.md` and follow ALL instructions in it.

---
*arra-oracle-skills-cli v26.5.16*
