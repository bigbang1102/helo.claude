---
description: v26.5.16 G-CMD | Show or bump CalVer version via bun scripts/calver.ts in arra-oracle-skills-cli. Default is dry-run --check. Read-only by default; `--apply` writes package.json. **For full release flow (commit + push + PR), use /release-alpha or /release-stable instead** — /calver alone never commits, pushes, or PRs.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /calver

Execute the `calver` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "calver"` instead of reading the file manually.

**Otherwise**: Read the skill file at `C:\Users\Dev\.claude\skills/calver/SKILL.md` and follow ALL instructions in it.

---
*arra-oracle-skills-cli v26.5.16*
