---
description: v26.5.16 G-CMD | Clone or create repos for active development — the right hand of /learn. Workflow modes — default (long-term dev), --flash (issue → PR → offload), --contribute (fork + multi-PR). Use when user says "incubate [repo]", "work on [repo]", "clone for dev", or wants to set up a dev workflow. Do NOT trigger for study/exploration (use /learn), finding projects (use /trace), or session mining (use /dig).
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /incubate

Execute the `incubate` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "incubate"` instead of reading the file manually.

**Otherwise**: Read the skill file at `C:\Users\Dev\.claude\skills/incubate/SKILL.md` and follow ALL instructions in it.

---
*arra-oracle-skills-cli v26.5.16*
