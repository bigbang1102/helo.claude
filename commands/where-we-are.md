---
description: v26.5.16 G-CMD | Session awareness — alias for /recap --now deep. Quick mid-session check of current topic, timeline, and pending items. Use when user asks "now", "where are we", "what are we doing". Do NOT trigger for full session start orientation (use /recap), "standup" (use /standup), or past session mining (use /dig).
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /where-we-are

Execute the `where-we-are` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "where-we-are"` instead of reading the file manually.

**Otherwise**: Read the skill file at `C:\Users\Dev\.claude\skills/where-we-are/SKILL.md` and follow ALL instructions in it.

---
*arra-oracle-skills-cli v26.5.16*
